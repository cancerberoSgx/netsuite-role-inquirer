{
  "name": "netsuite-role-inquirer",
  "version": "0.0.1",
  "description": "Let the user select a NetSuite role interactively using the command line",
  "main": "src/index.js",
  "bin": "src/bin.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sgurin",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^5.0.1",
    "request": "^2.83.0"
  }
}
